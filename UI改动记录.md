# UI改动记录

更新时间：2026-05-23

记录规则：
- 每次 UI 改动后追加一条一句话记录。
- 默认记录本地版本；如果已经上线，会在该条后标注“已上线”。

## 最近改动
- 管理后台首页模块右上角操作收敛为单个关闭按钮，并支持原生拖拽调整顺序。
- 管理后台首页顶部重置布局入口移除，改为左下角悬浮按钮。
- 管理后台首页统计能力收敛到首页概览，移除独立“统计分析”菜单。
- 管理后台首页补充量表结果统计和访问人员统计模块。
- 管理后台二维码列表弱化二维码 ID 与绑定档案两列，改为在老人基本信息主列中折叠展示细节。
- 管理后台二维码查看弹窗改为只展示当前已生效二维码，查看时不再隐式重新生成。
- 管理后台二维码停用和重新生成操作补充明确成功反馈，并同步刷新列表与弹窗状态。
- 管理后台志愿者与家属管理页补充创建时间、创建方式、邀请码等字段展示。
- 管理后台志愿者负责老人弹窗改为只展示当前负责老人，并拆分添加与删除入口，同时按一位老人仅能分配给一位志愿者处理。
- 管理后台志愿者编辑弹窗将“重置密码（可选）”文案改为“修改密码”。
- 管理后台各报表页统一补充字段折叠/展开能力，支持按页面记忆显示列并可一键重置字段布局。
- 管理后台角色权限页重构为纯账户权限管理表，按管理员、志愿者、家属分组展示账号并通过密码确认弹窗修改账号类型。
- 管理后台操作日志页将英文动作码统一映射为中文展示，筛选下拉、统计卡和表格记录的类型字段保持一致。
- 管理后台访问人员日志页继续补齐中文动作显示，访问类型分布统计图与下方记录表的类型字段统一使用中文标签。
- 管理后台访问人员日志页将访问对象从原始对象 ID 提升为老人姓名加档案号展示，并支持点击弹窗查看老人详情与本次访问信息。
- 管理后台首页模块右上角的拖动排序提示文案与残留样式彻底移除，仅保留关闭按钮和拖拽能力。
- 管理后台首页改为首屏核心数据优先加载、次级统计延后补齐，并为后台 GET 接口增加短时缓存以优化本地连远程后端时的加载速度。
- 管理后台首页移除独立的首页模块管理面板，仅保留模块拖拽、隐藏和左下角重置布局按钮。
- 管理后台角色权限页移除说明型大段文字与“审计员”角色，角色修改改为下拉变更后弹出确认并校验当前管理员账号密码。
- 管理后台操作日志页补充按人员、对象、来源 IP、类型、结果、关键词和日期范围筛选，并实现各页面 CSV 导出。
- 管理后台老人档案页中的用药信息和量表信息改为当前页直接跳转到对应编辑页。
- 管理后台量表编辑页补充陈德明的多条演示量表数据，用于支撑表格与首页统计展示。
- 管理后台志愿者列表改为直接展示负责老人明细、创建信息和账号信息，弱化零散字段列。
- 管理后台首页模块拖拽改为带占位高亮的实时换位交互，并新增左下角缩小、右下角放大的一档一档固定尺寸缩放，同时补齐桌面、平板、手机三档适配。
- 管理后台首页默认布局优化为顶部五张指标卡单行展示，并升级首页布局缓存版本，保证重置布局后回到新的初始排布。
- 管理后台首页模块缩放控件去掉加减图标，改为安卓小窗风格的角落式无字缩放手柄。
- 管理后台访问人员记录页的统计卡和报表区改为与首页一致的模块化布局，支持拖拽排序、固定档位缩放、隐藏与重置。
- 管理后台访问人员记录页优化为页面主体固定高度、最近记录与主表内部滚动的布局，桌面端减少整页下滚，移动端保留自然滚动。
- 管理后台登录与账号权限确认口径统一为管理员账号密码 `admin / admin`，移除对旧演示密码的兼容转换。
- 管理后台登录失败提示修正为优先显示账号密码错误等后端返回信息，不再将登录 401 一律折叠为“请求失败”。
- 管理后台壳层流式布局优化为左侧导航与右侧工作区同节奏收缩，中等宽度优先保持双栏结构，窄屏再切换为更紧凑的顶部导航流式排布。
- 管理后台最小屏宽下进一步压缩侧栏头部、菜单项高度和首页标题卡间距，减少顶部占高并让首页内容更早进入首屏。

## 后续约定
- 只要继续做 UI 改动，就在本文末尾追加一句。
- 若改动已上线，会在该句末尾标注“已上线”。
- 管理后台志愿者列表的创建时间改为优先显示本地真实记录，并对历史缺失时间的账号按本地元数据或 ID 时间戳进行稳定回填。
- 管理后台：字段折叠菜单补齐到账号权限管理页，并修正全局字段菜单组件的中文文案与重置文案。
- 管理后台首页与访问人员记录页移除模块拖拽排序、大小缩放及对应源码链路，模块恢复入口仅保留为“恢复模块”以重新显示被隐藏卡片。
- 管理后台首页与访问人员记录页进一步移除模块删除/隐藏、恢复模块及对应本地缓存逻辑，首页与日志概览区改为固定模块展示。
- 管理后台首页与访问人员记录页按内容密度重新固定模块排布，高信息密度模块改为整行展示，减少长模块与短模块并排导致的版面失衡。
- 管理后台列表页统一收紧筛选工具栏、分段切换、表格行高与操作按钮排布，减少按钮文字竖排和无效留白，让老人档案、志愿者、二维码等页面首屏承载更多信息。
- 管理后台首页将短指标卡从信息面板网格中拆出为独立流式指标区，避免“量表记录”等小卡被右侧高面板撑出大块空白。
- 管理后台左侧导航从整屏高卡片收紧为按内容自适应高度，减少底部大面积空白并压缩菜单项纵向占用。
- 管理后台左侧导航新增可收缩模式，收起后保留单列图标导航并记忆状态，为右侧内容区释放更多横向空间。
- 管理后台志愿者与家属管理页补齐页面级自适应布局，工具栏改为栅格式重排并为表格增加横向容纳与操作列压缩规则，保证中等宽度下右侧按钮仍可完整展示。
- 管理后台志愿者编辑弹窗改为回填真实手机号，并将账号、手机号、状态与可选密码修改统一打通到后端持久化更新链路。
- 管理后台“志愿者与家属管理”页新增第三个“邀请码管理”页签，并将原独立邀请码页抽成复用模块，保证在同一工作流中可直接查看邀请码、复制链接、生成邀请码与导出数据。
- 管理后台邀请码管理页新增“已邀请家属”展示与家属明细弹窗，可直接看到某个邀请码邀请到的家属姓名、手机号、关系、绑定时间和绑定老人信息。
- 统一后端新增家属与邀请码演示数据迁移，补充多组家属账号、邀请码和绑定关系，并将邀请码使用次数按真实有效绑定关系自动回填对齐。
- 管理后台志愿者负责老人与家属绑定审查弹窗改为内容区独立滚动、底部操作区固定可见，避免中低屏高下“保存负责老人/关闭”按钮被内容挤出视口。
- 管理后台前端补齐 401/403 会话失效处理：管理员接口返回 Forbidden/Unauthorized 时自动清理本地登录态并跳回登录页，避免页面停留在后台内连续显示后端权限错误。
- 管理后台邀请码管理补充“恢复”和“重新生成”操作：作废后可恢复原邀请码，也可为同一位老人重新生成一条新的随机英文数字邀请码，旧邀请码记录保留用于家属追踪。
- 管理后台邀请码管理改为复用线上已可用接口：作废与恢复统一走状态切换，重新生成直接为当前老人新增一条随机英文数字邀请码，并保留原邀请码记录与已邀请家属追踪关系。
- 管理后台短信中转页顶部“设备状态”摘要改为高密度并排卡组：首张卡放大设备总数，其余状态卡紧凑排列，减少首屏纵向占用并提前露出设备表与回传记录。
- 管理后台操作日志新增“家属操作”分类入口；家属登录、退出、查看绑定老人、查看老人详情、查看家属用药、查看家属二维码、修改联系人、维护用药等行为开始独立记录并展示。
- 管理后台志愿者“负责老人管理”弹窗改为“添加老人”模式下仅展示可添加老人，并新增右侧可收缩的已选老人姓名栏，在有限屏高与屏宽下更容易同时完成筛选、查看和保存。
- 管理后台首页顶部原先分散的五张核心指标卡合并为单一“核心数据总览”面板，在一个总框内集中展示老人、志愿者、二维码、日志与量表关键数字，减少首屏碎片感。
- 管理后台“编辑用药信息”弹窗由挤压式表格改为卡片式逐条编辑布局，字段菜单保留但每条用药独立成块，避免侧栏收缩或中等宽度下输入框与删除按钮错位拥挤。
- 管理后台短信中转页顶部设备摘要在窄宽度下改为三列两排展示，并同步压缩标签与备注字号，避免五条摘要继续一行一个地拉长首屏。
- 管理后台短信中转页整体改为更高密度的工作台布局：设备配置表单独进入紧凑滚动容器，短信回传记录与验证会话改为并排双栏展示，并统一压缩表格行高、输入框与操作按钮间距。
- 管理后台访问人员记录页进一步改为“工作区固定高度 + 子模块内部滚动”模式：访问类型、访问对象、按日期统计、最近访问记录和主记录表都改为各自面板内滚动，减少整页级上下滚动。
- 管理后台管理员操作、医护操作、家属操作三类审计页同步改为“筛选区固定 + 表格区内部滚动”模式，与访问人员记录页统一为子窗口滚动体验，减少整页级上下拖动。
- 管理后台审计页补加统一的固定高度页面壳层，真正锁定顶部标题与筛选区高度后再将滚动下放到各自表格/统计模块内部，修正此前“样式已改但整页仍在接管滚动”的问题。
- 管理后台首页“核心数据总览”进一步改为横向单排 5 个小组件展示，并通过横向容纳层避免中等宽度下自动塌成两列大卡，保持真正的仪表盘摘要条观感。
- 管理后台首页“访问人员统计”等行为分布图补齐家属、邀请、档案、用药等动作的中文映射，避免首页统计图里再直接显示英文枚举值。
- 管理后台通用“字段”菜单改为挂载到页面顶层的固定浮层，不再受面板裁切、工具栏宽度或右侧边界影响，统一修复量表、档案、二维码等多页面字段菜单展开异常。
- 管理后台管理员操作、医护操作、家属操作三类审计页移除筛选区下方重复出现的二级标题，仅保留顶部页面标题与记录总数，减少视觉重复。
- 管理后台管理员操作、医护操作、家属操作三类审计页补加展示层兜底规则，统一隐藏筛选区下方可能残留的直系重复标题，避免浏览器保留旧结构时仍出现“管理员操作/医护操作/家属操作”二次显示。
- 管理后台访问人员记录页顶部 4 张独立统计卡合并为一个总框内的横向 4 小组件摘要条，显著压缩首屏高度并减少模块碎片感；中等宽度保持一排 4 个，小屏再退化为两列或单列。
- 管理后台访问人员记录页调整模块顺序，将“最近访问记录”和“访问人员记录”提到顶部摘要下方优先展示，减少滚动后才能看到主表的情况。
