# 单元测试目录开发说明

## 当前状态

当前目录尚未落具体单元测试代码，但测试目标已经明确，不再是“仅保留占位说明”的阶段。

## 后续优先级

优先补这些测试：

- `SmsParser` 的短信格式解析测试
- `RelayPreferences` 的默认值与保存测试
- `SmsRelayRepository` 的状态流转测试
- `RelayApiService` 的请求构造测试

## 当前要求

- 新增测试时优先覆盖短信解析和状态变更
- 不要先写纯 UI 快照类测试
- 先把核心链路测稳：入列 -> 上传 -> 状态更新
