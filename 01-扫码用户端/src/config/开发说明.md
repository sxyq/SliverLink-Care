# config 目录开发说明

## 目录职责

`config` 存放扫码端环境配置和接口路径常量。

## 计划文件

| 文件 | 代码功能 | 状态 |
| --- | --- | --- |
| `env.ts` | 读取 `VITE_API_BASE_URL`、Mock 开关、微信 AppId | 已完成 |
| `endpoints.ts` | 集中维护扫码端后端接口路径 | 已完成 |

## 注意事项

- 不在代码中硬编码生产域名。
- 不在前端保存 AES 密钥、短信密钥、JWT 签名密钥。

