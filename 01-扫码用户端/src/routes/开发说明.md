# routes 目录开发说明

## 目录职责

`routes` 存放扫码端路由配置。

## 计划文件

| 文件 | 代码功能 | 状态 |
| --- | --- | --- |
| `router.tsx` | 定义 `/s/:encryptedQrToken`、`/verify`、`/archive`、`/medications`、`/scales` 等页面路由 | 已完成 |

## 注意事项

- `encryptedQrToken` 只透传给后端。
- 无效 token 跳转错误页。

