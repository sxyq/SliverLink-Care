# Secrets and local editor state
.ssh-check/
.vscode/
*.pem
id_rsa
local.properties
.env.local

# Logs and temporary files
*.log
dev-server*.out.log
dev-server*.err.log
_local_run_logs/
_smoke_artifacts/
_server_smoke_artifacts/
tmp*.py

# Frontend dependencies and build outputs
node_modules/
dist/
coverage/
.swc/
*.tsbuildinfo

# Java backend build outputs
target/

# Android build outputs
.gradle/
.kotlin/
build/
app/build/

# Local development workspace artifacts (per-developer, not source)
12_workspace/

# IDE / AI assistant working state
.trae/
.agent/
.codegraph/

# Test reports (functional/integration/performance/regression/security/unit)
# These are reproducible artifacts generated by `06-测试与质量保障/` scripts;
# they should be archived locally or in CI, not committed.
06-测试与质量保障/reports/

# Bundled Maven distribution used by the unified backend.
# Keep the wrapper (.mvn/wrapper/) so the standard `mvnw` works,
# but the extracted toolchain should live outside the repo.
04-统一后端/.tools/

# OS files
Thumbs.db
.DS_Store
