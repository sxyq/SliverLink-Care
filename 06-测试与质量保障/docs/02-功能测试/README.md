# 功能测试

**最后更新**：2026-05-31

---

## 1. 当前已完成内容

### 1.1 线上只读功能烟测

已执行 5 轮线上只读 GET 冒烟，覆盖三个前端入口页面：

| 入口 | URL | 最近结果 | 最近延迟 |
|------|-----|---------|---------|
| 扫码用户端 | `/silverlink/scan/` | 200 ✅ | 332ms |
| 管理后台端 | `/silverlink/admin/` | 200 ✅ | 44ms |
| 志愿者填写端 | `/silverlink/volunteer/` | 200 ✅ | 42ms |

**最近一次通过时间**：2026-05-26T00:48:46Z

### 1.2 线上 API 功能烟测（33 条）

在首轮完整回归中执行了 33 条 API 级功能烟测，全部通过：

- 管理员签名登录
- 未登录访问后台数据被 403 拒绝
- 管理后台首页数据、老人档案、志愿者、家属绑定、邀请码、审计日志、审核请求、短信中转记录/设备/session
- RBAC 角色与权限读取
- 二维码列表读取、二维码 token 解析
- 扫码身份验证、验证后基础信息/健康档案/用药/量表读取
- 二维码解析老人、身份验证 session、验证后基础信息三者一致性断言
- 同一个验证 session 尝试跨老人读取，被 403 拒绝
- 志愿者登录、个人资料、本人负责老人、本人老人二维码管理
- 家属登录、本人绑定老人、老人详情、用药、二维码
- 邀请码预览

**关键断言**：
- QR resolved elder = `elder-002`
- verified session = `scan-session-1779710424666`
- volunteer own elder = `elder-1779595988004`
- family own elder = `elder-001`

### 1.3 线上写操作功能测试

已新增两轮“可逆 + 深回归写操作”功能测试，确保在不污染线上数据的前提下，补齐关键写链路证据：

第一轮可逆写操作：

1. 管理员创建邀请码并立即删除
2. 管理员切换邀请码停用状态并恢复
3. 家属修改联系人信息并恢复原值

第二轮深回归写操作：

1. 管理员创建临时老人并确认自动生成二维码
2. 管理员为临时老人创建邀请码并成功预览
3. 志愿者使用邀请码完成注册并登录
4. 志愿者对临时老人执行二维码查看、重新生成、停用申请
5. 管理员审批二维码停用申请，确认二维码状态变为 `DISABLED`
6. 家属账号跨老人访问该临时老人，确认返回 `403`
7. 管理员直接创建临时志愿者并删除
8. 管理员删除临时注册志愿者、删除邀请码、删除临时老人，并确认均进入清理后状态

**结果**：

- 可逆写操作：`3/3` 通过
- 深回归写操作：`7/7` 通过

**最近一次通过时间**：

- 可逆写操作：2026-05-30T07:27:22Z
- 深回归写操作：2026-05-30T22:52:05Z

### 1.4 浏览器级真实交互冒烟

已通过 Codex in-app browser 执行真实浏览器交互，并补到多角色核心页：

1. 家属端真实登录成功并进入“已绑定老人”首页
2. 管理后台真实登录成功并进入系统首页仪表盘
3. 志愿者端真实登录成功并进入已登录列表页
4. 浏览器层 XSS 验证已证明联系人 payload 只按文本渲染，不执行脚本

这轮结果属于**真实浏览器交互证据**，但仍不是独立 Playwright CLI 回放矩阵，因为当前仍缺可用的 CLI 浏览器运行时。

---

## 2. 当前保留的关键基线

| 基线 | 路径 |
|------|------|
| 当前功能测试主文档 | `06-测试与质量保障/docs/02-功能测试/README.md` |
| 测试日志吸收与代码落实总审计 | `06-测试与质量保障/docs/00-总览/03-测试日志吸收与代码落实总审计.md` |
| 功能写操作脚本（可逆） | `06-测试与质量保障/scripts/functional/live_write_reversible_smoke.mjs` |
| 功能写操作脚本（深回归） | `06-测试与质量保障/scripts/functional/live_write_deep_regression.mjs` |
| 线上辅助脚本 | `06-测试与质量保障/scripts/common/live_api_helpers.mjs` |

---

## 3. 历史发现的问题

| 问题 | 发现时间 | 状态 |
|------|---------|------|
| 第一版烟测中志愿者/家属接口 403 | 2026-05-25 | ✅ 已修复（测试数据不匹配权限，非业务缺陷） |
| 管理后台接口必须携带签名头 | 2026-05-25 | ✅ 符合预期（无签名返回 missing signature headers） |

---

## 4. 当前未覆盖的功能场景

1. **家属邀请码完整注册闭环**：当前深回归覆盖了志愿者邀请码注册，家属邀请码注册仍缺短信验证码闭环
2. **并发场景**：功能烟测仍以串行为主，未覆盖并发操作下的数据一致性
3. **跨端联动**：未验证扫码端验证后志愿者端数据同步
4. **E2E 自动化矩阵**：已有真实浏览器交互证据，但仍无 Playwright/Cypress 级自动化回放

---

## 5. 当前阻塞

- 本机缺少可用 Playwright CLI 浏览器运行时
- 家属邀请码注册流程依赖短信验证码，当前仍需要可控测试号码或测试环境配合

---

## 6. 下一步建议

1. 在线上或预发环境补“家属邀请码注册 -> 家属登录 -> 联系人更新”的完整浏览器闭环
2. 增加跨端联动验证（扫码验证 → 志愿者端数据同步）
3. 在具备可用浏览器 CLI 运行时后，补 Playwright/Cypress 自动化矩阵
