# 测试总规划

## 1. 测试目标

本目录用于建立智联名牌项目的长期质量保障体系，覆盖功能、单元、集成、性能、安全权限和回归测试。测试资料、脚本、报告统一放在 `06-测试与质量保障/`，避免散落在各端工程里。

## 2. 分类目录

| 分类 | 文档目录 | 脚本目录 | 报告目录 | 目标 |
| --- | --- | --- | --- | --- |
| 总览 | `docs/00-总览` | `scripts/common` | `reports/` | 测试基线、执行顺序、报告规范 |
| 单元测试 | `docs/01-单元测试` | `scripts/unit` | `reports/unit` | 每个函数/方法/组件行为的最小粒度验证 |
| 功能测试 | `docs/02-功能测试` | `scripts/functional` | `reports/functional` | 三端页面和主要业务流程可用 |
| 集成测试 | `docs/03-集成测试` | `scripts/integration` | `reports/integration` | 前端、后端、数据库、短信中转串联正确 |
| 性能测试 | `docs/04-性能测试` | `scripts/performance` | `reports/performance` | 页面、接口、构建产物、PDF 生成性能基线 |
| 安全与权限测试 | `docs/05-安全与权限测试` | `scripts/security` | `reports/security` | 权限、隐私、签名、防串档、防越权 |
| 回归测试 | `docs/06-回归测试` | `scripts/regression` | `reports/regression` | 每次修复和部署前的稳定性确认 |

## 3. 测试对象

| 模块 | 路径 | 测试重点 |
| --- | --- | --- |
| 扫码用户端 | `01-扫码用户端` | 二维码解析、验证会话、隐私展示、防串档 |
| 志愿者/家属端 | `02-志愿者填写端` | 邀请码注册、档案填写、二维码查看、家属协管 |
| 管理后台端 | `03-管理后台端` | 登录、权限、表格展示、审核、二维码、审计 |
| 统一后端 | `04-统一后端` | API、鉴权、数据库、二维码 token、短信中转、名牌 PDF |
| 安卓短信中转端 | `05-安卓短信中转端` | 短信解析、签名上传、配置同步、前台服务 |
| 微信小程序端 | `08-微信小程序端` | Taro/微信平台适配、扫码入口、验证会话、工作台权限、分包构建产物 |

## 4. 第一轮推荐执行顺序

1. 固定测试基线：记录 Git commit、工作区状态、服务地址、数据库环境。
2. 单元测试规划落地：先补工具函数、API 映射函数、后端 service、安卓 util 的单元测试。
3. 构建门禁：三个 Web 前端 `npm run build`，微信小程序 `typecheck/build:weapp/test:route-contract/test:platform-contract/test:backend-contract/test:page-privacy-render/test:dist-security/test:artifact/test:performance-budget`，后端 Maven 测试/打包，安卓 JVM 测试/构建。
4. 功能冒烟：扫码端、微信小程序扫码入口、志愿者/家属端、微信小程序工作台、管理后台核心页面逐个打开和交互；若本轮目标限定为代码层测试，则以构建产物、路由/平台/后端契约和页面隐私静态审计替代界面点击证据。
5. 专项回归：近期修复的扫码错人、二维码按钮、邀请码注册、后台表格展示、小程序分包页面缺失和微信平台能力失败。
6. 集成测试：二维码解析到验证再到受保护档案读取的完整链路，同时覆盖 Web 与微信小程序 service 契约。
7. 性能基线：构建体积、页面加载、小程序分包体积、关键接口响应、PDF 生成耗时。
8. 生成报告：每轮输出 `summary.md`、`failures.md`、`commands.log` 和性能数据；运行时/UI 专项另补截图或录屏证据。

## 5. 报告规范

每轮测试报告建议使用：

```text
06-测试与质量保障/reports/<分类>/YYYYMMDD-HHMM-测试主题/
```

每轮至少包含：

- `summary.md`: 总结、结论、风险。
- `commands.log`: 关键命令、退出码、日志摘要。
- `failures.md`: 失败项、复现步骤、相关文件、建议修复。
- `screenshots/`: 前端和移动端截图证据；代码层单元/性能/安全报告可只保留构建二维码或其他非交互图片证据。
- `performance.json`: 性能指标。

## 6. 当前已知风险

- 管理后台 `admin-layout.css` 曾出现 CSS 结构损坏，后台 UI 测试前应先确认构建无 CSS 语法警告。
- 前端当前没有单元测试脚本，需先引入 Vitest + Testing Library，才能执行函数级单元测试。
- 安卓仪器测试依赖模拟器或真机，应与 JVM 单元测试分开执行。
- 微信小程序端本地 `unit/static/route-contract/platform-contract/backend-contract/page-privacy-render/typecheck/build/artifact/security/performance` 门禁可闭环代码层单元、性能、安全目标；若另开运行时/UI 专项，才需要微信开发者工具或真机预览证据确认平台 API、合法域名、扫码和分包加载行为。
- 当前工作区存在非本测试目录的未提交/删除状态，正式测试前需要明确是否纳入本轮基线。
